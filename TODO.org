# TODO.org
#+TITLE: FastCode Native - Project TODO
#+AUTHOR: FastCode Team

* Features to Implement

** TODO Markdown responses parsing and styling
Need to parse markdown formatting in AI responses (bold, italic, code blocks, lists, links) and render them with appropriate styling in the chat display.

** TODO Line-by-line responses rendering (streaming)
Investigate how opencode.ai handles streaming responses:
- Does the API support streaming/chunked responses?
- How is partial content delivered (Server-Sent Events, chunked JSON, etc.)?
- Can we render content as it arrives instead of waiting for full response?
- What is the latency improvement?
- Update streaming_text_ctrl to support incremental text updates

** TODO Keeping the context of the conversation
Investigate how opencode.ai actually maintains conversation context:
- Does it send full message history with each request?
- Is there a session/conversation ID mechanism?
- How does it handle context window limits?
- Check the API documentation and actual request/response patterns

** TODO Handling tool use and web searches
Investigate how opencode.ai implements tool use:
- What tools are available (web search, file operations, etc.)?
- How are tool calls represented in the API?
- How are tool results sent back to the model?
- Is there a specific tool use format in requests/responses?

** TODO Handling subagents
Investigate how opencode.ai handles subagent orchestration:
- How are subagents defined and invoked?
- What is the communication protocol between main agent and subagents?
- How is subagent output integrated into the main response?
- Are there special API endpoints or message formats for subagents?

* Research Tasks

** INVESTIGATE OpenCode Zen API documentation
- [ ] Find official API docs or schema
- [ ] Analyze actual request/response payloads
- [ ] Document conversation context mechanism
- [ ] Document tool use protocol
- [ ] Document subagent protocol

** INVESTIGATE Cutescroll tutorial for text styling
The cutescroll project in parent directory might have examples of:
- Rich text rendering
- Styled text blocks
- Mixed content types

* Implementation Notes

** Markdown Parsing Options
- Use existing library (md4c, cmark, etc.)
- Implement lightweight parser for common markdown features
- Map markdown elements to BlockType in streaming_text_ctrl

** Conversation Context
Options:
1. Store full message history locally and send with each request
2. Use server-side session management (if API supports it)
3. Hybrid approach with periodic summarization

** Tool Use Architecture
Need to design:
- Tool registry/discovery mechanism
- Tool call serialization format
- Tool result handling
- Async tool execution

** Subagent Architecture
Need to understand:
- Are subagents separate API calls or part of main request?
- How is subagent output tagged/formatted?
- Error handling for subagent failures

* Completed Tasks

** DONE Fix crashes when changing API key
** DONE Add debug panel for JSON traffic
** DONE Fix UTF-8 response handling
** DONE Fix all compiler warnings
